@{
    ViewData["Title"] = "Wallets";
}

<div class="row">
    <div class="col-12">
        <div class="page-title-box">
            <h1 class="display-4">Wallets</h1>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <div class="table-responsive">
                    <table id="RefferalsUsers" class="table table-striped table-centered mb-0">
                        <thead>
                            <tr class="table-primary">
                                <th>Id</th>
                                <th>Currency</th>
                                <th>Created(UTC)</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Wallets)
                            {
                                <tr>
                                    <td>
                                        @item.Id
                                    </td>
                                    <td>
                                        @item.CurrencyAcronim
                                    </td>
                                    <td>
                                        @item.Created.ToString("yyyy-MM-dd HH:mm:ss")
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
            </div>
        </div>
    </div>
</div>